# Commissioner panel hotfix

This package forces the updated `scoreboard.html` commissioner panels and cache-busts all local assets.

Expected live text:

```text
Commissioner Local Test Import
Commissioner Lock / Public Export
Copy Public Players Block
Copy Event Lock Patch
```

Version key:

```text
20260711-commissioner-panel-hotfix1
```

If the live page still shows only `Copy Current Players JSON`, GitHub Pages is serving an old `scoreboard.html` or the repo root was not overwritten with this package.
