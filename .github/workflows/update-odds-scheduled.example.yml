# Optional scheduled odds updater.
# Rename this file to update-odds-scheduled.yml to enable.
#
# Warning: check your odds API request quota before enabling.
#
# name: Scheduled Odds Snapshot
#
# on:
#   schedule:
#     - cron: "0 */2 * * *"
#   workflow_dispatch:
#
# permissions:
#   contents: write
#
# jobs:
#   update-ufc-odds:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#       - name: Pull UFC 324 odds
#         env:
#           ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
#         run: node tools/pull-odds.mjs ufc-324 mma_mixed_martial_arts
#       - name: Commit odds snapshot
#         run: |
#           git config user.name "github-actions"
#           git config user.email "github-actions@github.com"
#           git add events/odds/
#           git commit -m "Update scheduled odds snapshot" || echo "No odds changes"
#           git push
